rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    // NEXORA V3.3 uses Google Drive public links for product media.
    // Firebase Storage uploads are disabled to avoid dependency on Storage billing/availability.
    match /{allPaths=**} {
      allow read: if true;
      allow write: if false;
    }
  }
}
